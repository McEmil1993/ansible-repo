---
- name: Update apt cache
  apt:
    update_cache: yes

- name: Install MySQL server
  apt:
    name: mysql-server-8.0
    state: present
  ignore_errors: yes  # Ignore errors temporarily for troubleshooting

- name: Ensure MySQL service is running and enabled
  service:
    name: mysql
    state: started
    enabled: yes
  ignore_errors: yes  # Ignore errors temporarily for troubleshooting

- name: Deploy MySQL configuration file
  copy:
    src: ../files/mysqld.cnf
    dest: /etc/mysql/mysql.conf.d/mysqld.cnf
    mode: 0644
    owner: mysql
    group: mysql
  notify:
    - Restart MySQL

# Execute the bash script to create MySQL user 'manager'
- name: Create MySQL user 'manager' using bash script
  script: bash ../files/create_mysql_user.sh
  args:
    executable: /bin/bash
    
# - name: Create MySQL user 'manager' with all privileges using SQL query
#   mysql_query:
#     login_user: root  # Use appropriate MySQL root user credentials
#     login_password: "{{ mysql_root_password }}"  # Define mysql_root_password in your Ansible vault or as a variable
#     query: "GRANT ALL PRIVILEGES ON *.* TO 'manager'@'%' IDENTIFIED BY '12345';"

#   notify:
#     - Restart MySQL

# handlers:
#   - name: Restart MySQL
#     service:
#       name: mysql
#       state: restarted